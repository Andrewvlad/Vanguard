# Web server
PORT=3000

# DB (Postgres)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=vanguard

# Redis (BullMQ + cache)
REDIS_HOST=localhost
REDIS_PORT=6379

# AWS
AWS_REGION=us-east-1

# S3 bucket (image uploads)
S3_UPLOAD_BUCKET=vanguard-uploads

# Lambda functions
LAMBDA_PLATE_OCR=plate-ocr